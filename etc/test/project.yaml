project_name: Test
description: Test Project

definitions:
  - type: a5sql_mk2
    filename: ./dbgear.a5er
    mapping:
      MAIN: main
  - type: selectable
    prefix: _select
    items:
      y_or_n: Yes or No
      property_keys: システムプロパティキー

bindings:
  uuid:
    type: fixed
    value: UUID()
  now:
    type: fixed
    value: NOW()
  update_user:
    type: fixed
    value: SYSTEM
  set_y:
    type: fixed
    value: Y
  set_n:
    type: fixed
    value: N
  gen_uuid:
    type: call
    value: uuid

rules:
  update_user: update_user
  update_date: now
  .*_flag: y_or_n

deployments:
  localhost: mysql+pymysql://root:password@denv.host?charset=utf8
