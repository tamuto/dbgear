project_name: Test
description: Test Project

definitions:
  - filename: ./dbgear.a5er
    type: a5sql_mk2
    mapping:
      MAIN: main

rules:
  update_user: update_user
  update_date: now
  .*_flag: y_or_n

deployments:
  localhost: mysql+pymysql://root:password@denv.host?charset=utf8
